import {
  StyleSheet,
} from 'react-native';

export const colors = {
  light_red: '#F0141E',
  red: '#B50F16',
  dark_red: '#A5060D',
  black: '#303030',
};

const styles = StyleSheet.create({
  container: {
    flex: 1,
  },
});

export default styles;
